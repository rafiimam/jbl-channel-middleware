// Channel Middleware â€” sanitized sample. No bank data.

function describe() {
  return {
    system: "Channel Middleware",
    organization: "Jamuna Bank PLC",
    public: true,
    containsSecrets: false,
  };
}

module.exports = { describe };
